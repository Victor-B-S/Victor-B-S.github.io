import styled from "styled-components";

export const Wrapper = styled.div`
    display: flex;
    font-size: 1rem;
    color: red;
    padding: 3rem;
    justify-content: center;

    .repository-list{
        padding-left: 100px;
    }
`;